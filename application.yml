
server:
  port: 8191
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dumindanirosh/ceylontutors-cloud-config-store
          search-paths:
            - '*service'
